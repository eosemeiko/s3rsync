# =============================================================================
# ИСХОДНОЕ ХРАНИЛИЩЕ (SOURCE)
# =============================================================================

SOURCE_AWS_ACCESS_KEY_ID=your_access_key
SOURCE_AWS_SECRET_ACCESS_KEY=your_secret_key
SOURCE_BUCKET_NAME=source-bucket

# Опциональные параметры
# SOURCE_AWS_REGION=us-east-1
# SOURCE_ENDPOINT_URL=https://s3.amazonaws.com
# SOURCE_VERIFY_SSL=true


# =============================================================================
# ЦЕЛЕВОЕ ХРАНИЛИЩЕ (TARGET)
# =============================================================================

TARGET_AWS_ACCESS_KEY_ID=your_access_key
TARGET_AWS_SECRET_ACCESS_KEY=your_secret_key
TARGET_BUCKET_NAME=target-bucket

# Опциональные параметры
# TARGET_AWS_REGION=us-west-2
# TARGET_ENDPOINT_URL=https://s3.amazonaws.com
# TARGET_VERIFY_SSL=true


# =============================================================================
# НАСТРОЙКИ ПРОИЗВОДИТЕЛЬНОСТИ
# =============================================================================

# Количество параллельных операций (asyncio корутин)
#
# Рекомендации для разных систем:
#
# 🖥️  4 CPU / 10GB RAM (ваш случай):
#     100-150  - оптимально ⭐
#     150-200  - агрессивно, но безопасно
#     200+     - только если файлы маленькие (<1MB)
#
# 💻 2 CPU / 4GB RAM:
#     50-80    - безопасно
#
# 🚀 8+ CPU / 32GB+ RAM (сервер):
#     300-500  - максимальная скорость
#
# Почему можно много?
# - asyncio не создает реальные потоки/процессы
# - Каждая корутина занимает ~2KB памяти
# - 200 корутин = ~400KB + файлы в памяти
# - При средних файлах 125KB: 200 × 125KB = 25MB
# - Защита от больших файлов встроена (>10MB обрабатываются отдельно)
#
MAX_WORKERS=150


# =============================================================================
# ПРИМЕРЫ ENDPOINT ДЛЯ ПРОВАЙДЕРОВ
# =============================================================================

# MinIO:          http://localhost:9000 (+ VERIFY_SSL=false)
# Wasabi:         https://s3.wasabisys.com
# DigitalOcean:   https://nyc3.digitaloceanspaces.com
# Backblaze B2:   https://s3.us-west-002.backblazeb2.com
# Cloudflare R2:  https://<account-id>.r2.cloudflarestorage.com
# Yandex Cloud:   https://storage.yandexcloud.net
# Selectel:       https://s3.ru-1.storage.selcloud.ru
# FirstVDS:       https://s3.firstvds.ru (+ VERIFY_SSL=false)

# S3 Sync - Синхронизация файлов между S3 бакетами

Скрипт для копирования всех файлов с одного S3 бакета на другой без локального сохранения файлов (streaming). Поддерживает разные AWS аккаунты, многопоточность и проверку размера файлов.

## 🚀 Возможности

- ✅ Копирование файлов через память (без сохранения на диск)
- ✅ Поддержка разных AWS аккаунтов (разные credentials для источника и назначения)
- ✅ Многопоточное копирование для ускорения процесса
- ✅ Проверка существования файлов и их размера
- ✅ Пропуск уже скопированных файлов с совпадающим размером
- ✅ Прогресс-бар для отслеживания процесса
- ✅ Минимальное логирование (только важная информация)
- ✅ Автоматическая обработка ошибок

## 📋 Требования

- Python 3.12+
- Доступ к исходному и целевому S3 бакетам

## 🔧 Установка

1. Клонируйте репозиторий или скопируйте файлы:
```bash
git clone <repository_url>
cd s3sync
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` по примеру `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` своими credentials:
```env
# Исходный S3 бакет (источник)
SOURCE_AWS_ACCESS_KEY_ID=your_source_access_key
SOURCE_AWS_SECRET_ACCESS_KEY=your_source_secret_key
SOURCE_AWS_REGION=us-east-1
SOURCE_BUCKET_NAME=my-source-bucket

# Целевой S3 бакет (назначение)
TARGET_AWS_ACCESS_KEY_ID=your_target_access_key
TARGET_AWS_SECRET_ACCESS_KEY=your_target_secret_key
TARGET_AWS_REGION=us-west-2
TARGET_BUCKET_NAME=my-target-bucket

# Настройки копирования
MAX_WORKERS=10  # Количество потоков
```

## 🎯 Использование

Запустите скрипт:
```bash
python main.py
```

Скрипт:
1. Подключится к обоим S3 бакетам
2. Получит список всех файлов из исходного бакета
3. Для каждого файла:
   - Проверит существование в целевом бакете
   - Сравнит размеры
   - Скопирует файл, если он новый или размер отличается
4. Выведет итоговую статистику

## 📊 Пример вывода

```
🚀 Начало синхронизации
📤 Источник: my-source-bucket (us-east-1)
📥 Назначение: my-target-bucket (us-west-2)
🔧 Потоков: 10

📋 Получение списка файлов из бакета my-source-bucket...
✅ Найдено файлов: 150

📦 Копирование файлов...
100%|████████████████████████| 150/150 [00:45<00:00,  3.33файл/s]

============================================================
📊 ИТОГИ СИНХРОНИЗАЦИИ
============================================================
Всего файлов:      150
✅ Скопировано:    35
⏭️  Пропущено:      115 (уже существуют, размер совпадает)
❌ Ошибок:         0
============================================================

🎉 Синхронизация успешно завершена!
```

## ⚙️ Настройки

### MAX_WORKERS
Количество параллельных потоков для копирования. Рекомендуемые значения:
- `5-10` - для стабильного интернета
- `3-5` - для медленного интернета
- `10-20` - для быстрого интернета и большого количества файлов

### Ограничения
- Размер файлов: до 50 MB (оптимально для изображений и аудио)
- Для файлов > 5 GB может потребоваться multipart upload (не реализовано)

## 🛡️ Безопасность

- Файл `.env` автоматически игнорируется git
- Никогда не комитьте credentials в репозиторий
- Используйте IAM роли с минимальными необходимыми правами:
  - Исходный бакет: `s3:GetObject`, `s3:ListBucket`
  - Целевой бакет: `s3:PutObject`, `s3:GetObject`, `s3:ListBucket`

## 🔍 Обработка ошибок

Скрипт обрабатывает следующие ситуации:
- Отсутствие credentials или неверная конфигурация
- Недоступность бакетов
- Ошибки доступа к отдельным файлам (логирует и продолжает)
- Прерывание пользователем (Ctrl+C)

## 📝 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются!

# S3 Sync - Синхронизация файлов между S3-совместимыми хранилищами

Скрипт для копирования всех файлов с одного S3-совместимого хранилища на другое без локального сохранения файлов (streaming). Поддерживает AWS S3, MinIO, Wasabi, DigitalOcean Spaces, Backblaze B2, Cloudflare R2, Yandex Cloud, Selectel и другие S3-совместимые сервисы.

## 🚀 Возможности

- ✅ Копирование файлов через память (без сохранения на диск)
- ✅ Поддержка любых S3-совместимых хранилищ (AWS S3, MinIO, Wasabi, DO Spaces, B2 и др.)
- ✅ Разные провайдеры для источника и назначения (например, AWS → MinIO)
- ✅ Многопоточное копирование для ускорения процесса
- ✅ Проверка существования файлов и их размера
- ✅ Пропуск уже скопированных файлов с совпадающим размером
- ✅ Прогресс-бар для отслеживания процесса
- ✅ Минимальное логирование (только важная информация)
- ✅ Автоматическая обработка ошибок
- ✅ Поддержка кастомных endpoint URL
- ✅ Поддержка path-style и virtual-hosted-style addressing
- ✅ Опциональная проверка SSL сертификатов

## 📋 Требования

- Python 3.12+
- Доступ к исходному и целевому S3-совместимому хранилищу

## 🔧 Установка

1. Клонируйте репозиторий или скопируйте файлы:
```bash
git clone <repository_url>
cd s3sync
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` по примеру `.env.example`:
```bash
cp .env.example .env
```

4. Настройте credentials:

Отредактируйте `.env` файл, используя примеры из `.env.example`:
- Для **AWS S3** - базовая конфигурация
- Для **MinIO, Wasabi, DigitalOcean Spaces, Backblaze B2, Cloudflare R2, Yandex Cloud, Selectel и тп** - см. примеры в `.env.example`

> 💡 Вы можете мигрировать данные между любыми провайдерами: AWS → MinIO, Wasabi → DO Spaces и т.д.

## 🎯 Использование

Запустите скрипт:
```bash
python main.py
```

Скрипт:
1. Подключится к обоим S3 бакетам
2. Получит список всех файлов из исходного бакета
3. Для каждого файла:
   - Проверит существование в целевом бакете
   - Сравнит размеры
   - Скопирует файл, если он новый или размер отличается
4. Выведет итоговую статистику

## 📊 Пример вывода

```
🚀 Начало синхронизации
📤 Источник: my-source-bucket (https://s3.wasabisys.com, us-east-1)
📥 Назначение: my-target-bucket (http://localhost:9000, default)
🔧 Потоков: 10

📋 Получение списка файлов из бакета my-source-bucket...
✅ Найдено файлов: 150

📦 Копирование файлов...
100%|████████████████████████| 150/150 [00:45<00:00,  3.33файл/s]

============================================================
📊 ИТОГИ СИНХРОНИЗАЦИИ
============================================================
Всего файлов:      150
✅ Скопировано:    35
⏭️  Пропущено:      115 (уже существуют, размер совпадает)
❌ Ошибок:         0
============================================================

🎉 Синхронизация успешно завершена!
```

## 🌍 Поддерживаемые провайдеры

Скрипт совместим с любыми S3-совместимыми хранилищами:

## ⚙️ Настройки

### Обязательные параметры

#### Для источника (SOURCE):
- `SOURCE_AWS_ACCESS_KEY_ID` - Access Key ID
- `SOURCE_AWS_SECRET_ACCESS_KEY` - Secret Access Key
- `SOURCE_BUCKET_NAME` - Имя бакета

#### Для назначения (TARGET):
- `TARGET_AWS_ACCESS_KEY_ID` - Access Key ID
- `TARGET_AWS_SECRET_ACCESS_KEY` - Secret Access Key
- `TARGET_BUCKET_NAME` - Имя бакета

### Опциональные параметры

#### SOURCE_ENDPOINT_URL / TARGET_ENDPOINT_URL
Кастомный endpoint для S3-совместимых хранилищ.
- **AWS S3**: можно не указывать (используется стандартный)
- Для других провайдеров: см. примеры в `.env.example`

#### SOURCE_AWS_REGION / TARGET_AWS_REGION
Регион хранилища (опционально, зависит от провайдера).

#### SOURCE_ADDRESSING_STYLE / TARGET_ADDRESSING_STYLE
Стиль адресации S3:
- `virtual` (по умолчанию) - виртуальный хостинг: `bucket.s3.amazonaws.com`
- `path` - путевой стиль: `s3.amazonaws.com/bucket` (нужен для MinIO)

#### SOURCE_VERIFY_SSL / TARGET_VERIFY_SSL
Проверка SSL сертификатов:
- `true` (по умолчанию) - проверять сертификаты
- `false` - не проверять (для самоподписанных сертификатов или локального MinIO)

#### MAX_WORKERS
Количество параллельных потоков для копирования. Рекомендуемые значения:
- `5-10` - для стабильного интернета
- `3-5` - для медленного интернета
- `10-20` - для быстрого интернета и большого количества файлов

### Ограничения
- Размер файлов: до 50 MB (оптимально для изображений и аудио)
- Для файлов > 5 GB может потребоваться multipart upload (не реализовано)

## 🛡️ Безопасность

- Файл `.env` автоматически игнорируется git
- Никогда не комитьте credentials в репозиторий
- Используйте минимальные необходимые права доступа:
  - Исходное хранилище: `s3:GetObject`, `s3:ListBucket`
  - Целевое хранилище: `s3:PutObject`, `s3:GetObject`, `s3:ListBucket`
- Для production используйте IAM роли вместо статичных ключей (для AWS)
- Отключайте проверку SSL (`VERIFY_SSL=false`) только для локальной разработки

## 🔍 Обработка ошибок

Скрипт обрабатывает следующие ситуации:
- Отсутствие credentials или неверная конфигурация
- Недоступность бакетов
- Ошибки доступа к отдельным файлам (логирует и продолжает)
- Прерывание пользователем (Ctrl+C)
